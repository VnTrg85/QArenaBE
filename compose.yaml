services:
    mysql:
        image: "mysql:latest"
        environment:
            - "MYSQL_DATABASE=qarena"
            - "MYSQL_PASSWORD=123456"
            - "MYSQL_ROOT_PASSWORD=verysecret"
            - "MYSQL_USER=myuser"
        ports:
            - "65385:3306"
